<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="productDao">
	<insert id="insert">
		insert into product (no, item, cate, pric, opti, proop, descr, stock, regd, acti) values (product_seq.nextval, #{item}, #{cate}, #{pric}, #{opti}, #{proop}, #{descr}, #{stock}, systimestamp, 0)
		<selectKey keyProperty="no" resultType="Integer">
			select nvl (max(no), 0) from product
		</selectKey>
	</insert>
</mapper>